{
  "name": "the-game-vite",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 4000 && vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  }
}